# CareSphere Web UI

React 18 + TypeScript web application for laboratory technicians. Features real-time result review, differential keypad for manual counts, QC dashboards, and auto-verification workflows. Vite build, Material-UI components, React Query for data fetching.

**Run locally:** `npm install && npm run dev` (proxy to API gateway on localhost:3000)